---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: torconfigs.tor.fulvio.dev
spec:
  group: tor.fulvio.dev
  names:
    kind: TorConfig
    listKind: TorConfigList
    plural: torconfigs
    singular: torconfig
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TorConfig is the Schema for the TorConfigs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TorConfigSpec defines the desired state of TorConfig
            properties:
              contactInfo:
                type: string
              dirPort:
                type: integer
              image:
                type: string
              nickname:
                type: string
              orPort:
                type: integer
              relayType:
                type: string
            type: object
          status:
            description: TorConfigStatus defines the observed state of TorConfig
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
