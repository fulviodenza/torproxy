apiVersion: tor.fulvio.dev/v1alpha1
kind: TorConfig
metadata:
  labels:
    app.kubernetes.io/name: torproxy
    app.kubernetes.io/managed-by: kustomize
  name: TorConfig-sample
spec:
  # TODO(user): Add fields here
